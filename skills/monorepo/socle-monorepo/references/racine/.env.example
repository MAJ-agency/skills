# Variables d'environnement du monorepo — un bloc par service, ajouté par le
# skill qui pose le service. Aucun .env n'est nécessaire en local : chaque
# service embarque ses valeurs de développement. Pour des secrets locaux,
# copier ce fichier en .env (gitignored) — process.env garde toujours la priorité.
