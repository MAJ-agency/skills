
# ---------- Mobile (apps/mobile) ----------
# Origine de l'API vue de l'appareil. Sur un téléphone physique, localhost est
# le téléphone : mettre l'adresse LAN du poste dans apps/mobile/.env.local.
# Publique par construction (inlinée dans le bundle).
EXPO_PUBLIC_API_URL=http://localhost:3000
