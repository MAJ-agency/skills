
# ---------- Web (apps/web) ----------
# Origine de l'API vue du navigateur, hors développement (en dev, le client
# passe par le relais /api de next.config.ts). Publique par construction.
NEXT_PUBLIC_API_URL=http://localhost:3000
# Cible du relais /api en développement (côté serveur Next).
API_INTERNAL_URL=http://localhost:3000
