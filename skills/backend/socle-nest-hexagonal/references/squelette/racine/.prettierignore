node_modules
dist
build
.turbo
coverage
pnpm-lock.yaml

# Fichiers GÉNÉRÉS : les reformater les fait diverger de leur source et casse
# les gates de dérive (db:types:check, openapi:check).
apps/api/src/db/types.ts
apps/api/openapi.json

# Livrables HTML autoportés de la doc : artefacts figés, hors du gate format:check.
docs/**/*.html
